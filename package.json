{
  "name": "promptless-theme",
  "version": "1.3.3",
  "description": "Promptless WordPress Theme - Build scripts for asset optimization",
  "private": true,
  "scripts": {
    "build:css": "node scripts/build-css.js",
    "build:js": "node scripts/build-js.js",
    "build": "npm run build:css && npm run build:js && npm run build:rtl",
    "watch": "npm run build -- --watch",
    "build:rtl": "node scripts/build-rtl.js"
  },
  "devDependencies": {
    "cssnano": "^7.0.0",
    "postcss": "^8.4.35",
    "rtlcss": "^4.3.0",
    "terser": "^5.27.0"
  }
}
